# Development Environment Configuration
# This file is committed to the repository
# Safe to include development credentials (never used in production)

ENVIRONMENT=development

# Development Credentials (safe for local dev)
# In development mode, these defaults are used if not overridden
ADMIN_PASSWORD=admin123
JWT_SECRET_KEY=dev-secret-key-not-for-production-use

# JWT Settings
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Application Settings
LOG_LEVEL=DEBUG
