# Local Development Environment Configuration
# This file provides sensible defaults for local Docker Compose development
# For production deployment, use .env.example as a template and set proper secrets

# Environment: development (allows default JWT_SECRET_KEY and ADMIN_PASSWORD)
ENVIRONMENT=development

# Database URL is set in docker-compose.yml, but can be overridden here
# DATABASE_URL=postgresql://taskuser:taskpass@postgres:5432/tasktracker
