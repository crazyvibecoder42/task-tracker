# Backend Environment Variables
# Copy this file to .env and fill in the values

# Environment Configuration
# Set to 'dev', 'development', or 'local' for local development
# Set to 'production' or 'staging' for deployed environments
# Production-like environments (production, staging) enforce stricter security
# Default: production (requires ADMIN_PASSWORD to be set)
ENVIRONMENT=production

# Admin User Configuration
# Default admin credentials (created on first startup)
# Email: admin@example.com
# Password: Set via ADMIN_PASSWORD env var
# - In dev environments: defaults to 'admin123' if not set (convenience)
# - In production/staging: REQUIRED, startup will fail if not set (security)
# IMPORTANT: Always set a strong ADMIN_PASSWORD in production/staging!
# For local development, use .env.local.example as a template
ADMIN_PASSWORD=

# JWT Configuration
# JWT_SECRET_KEY: REQUIRED in production/staging (see security requirements above)
# CRITICAL: Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
JWT_SECRET_KEY=

# JWT Algorithm: Supported values: HS256, HS384, HS512 (default: HS256)
# JWT_ALGORITHM=HS256

# Access Token Expiry: Minutes (range: 1-1440, default: 15)
# Short-lived tokens for API requests, recommended: 15-60 minutes
# ACCESS_TOKEN_EXPIRE_MINUTES=15

# Refresh Token Expiry: Days (range: 1-90, default: 7)
# Long-lived tokens for obtaining new access tokens, recommended: 7-30 days
# REFRESH_TOKEN_EXPIRE_DAYS=7

# Database (usually set in docker-compose.yml)
# DATABASE_URL=postgresql://taskuser:taskpass@postgres:5432/tasktracker

# Optional: Email configuration (for future features)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=your-smtp-password
