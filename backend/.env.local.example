# Local Development Environment Variables
# Copy this file to .env for local development
# DO NOT use these settings in production or staging!

# Environment Configuration
# Set to 'dev', 'development', or 'local' for local development with relaxed security
# These environments allow default passwords and generate temporary JWT keys
ENVIRONMENT=dev

# Admin User Configuration
# Default admin credentials for local development (created on first startup)
# Email: admin@example.com
# Password: Set via ADMIN_PASSWORD env var
# In dev environments, this defaults to 'admin123' if not set
# WARNING: This is insecure! Only use for local development
ADMIN_PASSWORD=admin123

# JWT Configuration
# For local development, a temporary key is auto-generated if not set
# For production, ALWAYS set a secure key: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# JWT_SECRET_KEY=dev-local-insecure-key

# Optional JWT settings (defaults shown):
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=15
# REFRESH_TOKEN_EXPIRE_DAYS=7

# Database (usually set in docker-compose.yml)
# DATABASE_URL=postgresql://taskuser:taskpass@postgres:5432/tasktracker

# Optional: Email configuration (for future features)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=your-smtp-password

# Security Note:
# This configuration is designed for local development convenience.
# Production deployments should use .env.example as a template instead.
