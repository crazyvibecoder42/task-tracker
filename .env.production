# Production Environment Configuration
# This file is committed to the repository as a template
# DO NOT put secrets in this file - use .env.local instead

ENVIRONMENT=production

# CRITICAL SECURITY SETTINGS
# These MUST be set via .env.local (gitignored) or environment variables
# Startup will FAIL in production mode if these are not provided
#
# ADMIN_PASSWORD=  # REQUIRED - minimum 8 characters, cannot be "admin123"
# JWT_SECRET_KEY=  # REQUIRED - generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'

# Optional JWT Settings (defaults shown)
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=15
# REFRESH_TOKEN_EXPIRE_DAYS=7

# Application Settings
LOG_LEVEL=INFO
